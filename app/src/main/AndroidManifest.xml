<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uz.unzosoft.movie">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movie">
        <activity android:name=".ui.taskmovie.movie.EditMovieActivity"></activity>
        <activity android:name=".ui.taskmovie.movie.DescriptionActivity" />
        <activity android:name=".ui.taskmovie.movie.AddMovieActivity" />
        <activity android:name=".ui.taskmovie.movie.MoviesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.taskmovie.task.ThreeActivity" />
        <activity android:name=".ui.taskmovie.task.SecondActivity" />
        <activity android:name=".ui.taskmovie.task.FirstActivity" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>